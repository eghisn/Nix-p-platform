# Licensed Web Fonts

Place the licensed `.woff2` files for the current NIXP font stack here.

Expected filenames:

- `founders-grotesk.woff2`
- `founders-grotesk-text.woff2`

The CSS keeps the current font-family names and falls back to system fonts when these licensed files are not present.

